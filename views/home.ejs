<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body bgcolor="black">
    <%_ if(!sec){ %>
        sec is not set
    <%_ } else{ %>
        <%= sec.email %> ur sec is <%= sec.sec %>
    <%_ } %> <br>
    
    want to add sec <br>
    <a href="/logout">logout</a>
    <a href="/sec/<%= sec.email %>">add sec</a>
    <script>
        history.pushState(null, null, location.href);
        window.onpopstate = function () {
            history.go(1);
        };
    </script>
</body>
</html>